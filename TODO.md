# TODO

## Перед финальной сдачей

1. Проверить мобильную версию вручную:
   - главная;
   - каталог;
   - карточка работы;
   - карусель изображений;
   - форма заявки;
   - меню;
   - админка.

2. Проверить OpenGraph preview после деплоя:
   - Telegram;
   - VK.

3. Проверить production-сценарии:
   - главная открывается;
   - `/api/health` возвращает 200;
   - `/api/v1/health` возвращает 200;
   - `/api/v1/ready` возвращает 200;
   - отправка заявки работает;
   - Telegram-уведомление приходит;
   - загрузка изображения работает;
   - изображение открывается из S3/public URL;
   - админка открывается;
   - заявки отображаются, удаляются и меняют статус;
   - статистика открывается в админке;
   - UptimeRobot показывает все мониторы зелёными.

4. Проверить backup/restore drill на тестовой базе:
   - создать backup;
   - восстановить dump через `scripts/restore-postgres.sh`;
   - проверить таблицы после восстановления.

5. Настроить SSH hardening на VPS:
   - запретить password login;
   - оставить вход только по SSH key;
   - отключить root-login или ограничить его;
   - проверить firewall;
   - проверить fail2ban или аналог.

## На потом

1. Вернуть Playwright e2e, когда UI окончательно стабилизируется.
2. Добавить отдельную кнопку архивирования заявок, если удаление окажется слишком жёстким сценарием для реальной эксплуатации.
